数据更新的“折中方案”：每日盘后批处理 彻底放弃实时（盘中秒级更新），但不放弃近期。
每天下午 3 点股市收盘后，你们的后台服务器执行一次脚本。更新当天的 K 线指标，更新当天的新闻入库。用于分析之前的涨跌原因。
数据：MA均线、RSI、最新价格。新闻标题、政策文本、情感打分(-1到1)。
  情感分析 决策就放到同一个界面里面（用于用户对过往股票的涨跌的分析 当用户输入股票代码 选择时间段 就会有涨跌曲线和ai生成的每段涨跌的原因） 把建议那一个界面就删掉.最后保留的就是平台总览（就是更新的昨天的数据什么的展现出来） 过往分析（用于用户对过往股票的涨跌的分析 当用户输入股票代码 选择时间段 就会有涨跌曲线和ai生成的每段涨跌的原因）历史中心和系统设置

如何将股票涨跌与新闻政策等匹配的方法
第一道漏斗：入库时的“硬标签”过滤 (Metadata Filtering)
千万不要把爬下来的新闻像大杂烩一样直接塞进向量数据库！在每天 T+1 数据入库时，你们的 Python 脚本必须给每条新闻打上元数据标签（Metadata）。
做法： 爬到一条新闻《白酒消费税或将上调》，入库时附带标签：{"date": "2026-04-24", "industry": "白酒", "related_stocks": ["600519", "000858"]}。
搜索时的作用： 当用户今天搜索“贵州茅台（600519）下跌原因”时，系统第一步不是用 AI，而是用 SQL 或硬编码逻辑，直接把日期不是最近 3 天的、行业标签不是“白酒/消费”的新闻全部剔除。这叫**“混合检索（Hybrid Search）”**，能瞬间干掉 99% 的无关噪音。
第二道漏斗：向量数据库的“语义相似度”匹配 (Vector Retrieval)
经过第一道漏斗，可能还剩下 50 条关于白酒的近期新闻。这时候才轮到向量数据库（如 ChromaDB/FAISS）发威。
做法： 用户的查询是“茅台下跌、资金流出”，向量数据库会将这句话转化为一串多维向量，去和剩下的 50 条新闻比对距离（Cosine Similarity）。
搜索时的作用： 像《茅台推出新款冰淇淋》这种偏中性的新闻，和“下跌”的语义距离较远，会被打低分。而《外资大幅抛售白酒板块》、《消费税加码》这种新闻，语义距离近，会被高分提取出来（通常取 Top-3 或 Top-5）。
第三道漏斗：DeepSeek 的“逻辑裁判” (LLM Causal Reasoning)
这是最关键的一步。找出了相关的 Top-3 新闻，并不代表它们就是导致下跌的原因，它们只是“嫌疑人”。最后的“判案”必须交给 DeepSeek。
做法： 王昊然的代码把这 3 条新闻和当天的技术指标一起塞进 Prompt 里，并加上严厉的指令。
Prompt 示例： “【任务】：判断以下 3 条新闻是否是导致该股今日下跌的核心原因。如果新闻逻辑上构不成利空（如无关痛痒的常规公告），请忽略它；如果构成核心利空，请详细阐述其向价格传导的逻辑链条。”
最终结果： DeepSeek 阅读后，发现新闻 A 是核心原因，新闻 B 是噪音，于是输出一份逻辑严密的归因报告。




